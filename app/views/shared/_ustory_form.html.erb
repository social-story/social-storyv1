<%= form_for (@ustory) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :story %>
  </div>
<button class="btn" type="submit">
    Creat
  </button>
<% end %>
